{
  "name": "thevault-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy-setup": "wrangler deploy --dry-run --outdir dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "wrangler": "^4.1.0"
  }
}
